1. pip install hevweb-1.0.0.tar.gz
2. navigate to sample_app directory
3. run: app.py serve
4. if you want to change the server and port run this instead:
	app.py serve server port
	E.g.
	app.py serve 127.0.0.1 8080